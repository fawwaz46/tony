.venv/
__pycache__/
*.pyc
.env
*.egg-info/

# tony's own output, when reviewing this repo
.tony/

# local MCP tool state
.project-planner/

# site build output
web/.vercel/
web/dist/
web/.astro/
dist/
build/

# `vercel deploy` from the repo root leaves one of these behind; the linked
# project lives in web/, which is already ignored above.
.vercel/

# The dev virtualenv resolver writes this; CI installs with pip, so nothing reads it.
uv.lock
