.venv/
__pycache__/
*.pyc
.env
*.egg-info/

# tony's own output, when reviewing this repo
.tony/

# local MCP tool state
.project-planner/

# the dev viewer's scratch payload: `tony --viewer` fills it with a review of
# whatever repo it was run in, which is never ours to commit
web/src/fixtures/local.json

# site build output
web/.vercel/
web/dist/
web/.astro/
dist/
build/
